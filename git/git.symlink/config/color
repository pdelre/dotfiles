[color]
    ui = auto
[color "branch"]
    current = cyan bold
    local = green bold
    remote = red bold
    upstream = red bold
; Now managed by delta in diff
; [color "diff"]
;     meta = magenta bold
;     frag = yellow
;     func = cyan bold
